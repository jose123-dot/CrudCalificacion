import express from "express";
const router = express.Router();

router.get("/asignatura", () => {});
router.post("/asignatura", () => {});
router.delete("/asignatura/:id", () => {});
router.put("/asignatura/:id", () => {});

export default router;
